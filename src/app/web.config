<configuration>
  <system.webServer>
    <rewrite>
    <rules>
      <rule name="redirect all requests" stopProcessing="true">
        <match url=""
      </rule>
    </rules>
    </rewrite>
  </system.webServer>
</configuration>
