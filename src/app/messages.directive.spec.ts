import { MessagesDirective } from './messages.directive';

describe('MessagesDirective', () => {
  it('should create an instance', () => {
    const directive = new MessagesDirective();
    expect(directive).toBeTruthy();
  });
});
